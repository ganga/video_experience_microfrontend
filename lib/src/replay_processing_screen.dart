import 'package:flutter/material.dart';

class ReplayProcessingScreen extends StatelessWidget {
  const ReplayProcessingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Replay is still processing.'),
      ),
    );
  }
}
